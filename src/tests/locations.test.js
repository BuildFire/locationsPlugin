import { logInfo, test } from './helpers';

class LocationsTests {
  static run() {
    logInfo("Running Locations Tests");

    test("Getting Locations Info From publicData", () => {

    });

    test("Inserting a new Location to publicData", () => {

    });

    test("Update Location", () => {

    });

    test("Delete Location", () => {

    });
  }
}

export default LocationsTests;
