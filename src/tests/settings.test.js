import { logInfo, test } from './helpers';

class SettingsTests {
  static run() {
    logInfo("Running Settings Tests");

    test("Save settings to Datastore", () => {

    });

    test("Getting settings from Datastore", () => {

    });
  }
}

export default SettingsTests;
